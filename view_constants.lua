local view_constants = {
    HALF_TILE_WIDTH = 256,
    HALF_TILE_HEIGHT = 128,
    V_STEP = 58,
    INITIAL_ZOOM = 2 -- Initial zoom is the zoom level at which sprites images are displayed 1 to 1 in relation to screen resolution
}

return view_constants